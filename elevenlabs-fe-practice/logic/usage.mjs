// YOUR TASK: aggregate character usage per voice.
// events: [{ voiceId, characters }]  ->  [{ voiceId, total }]
// sorted by total DESC, then voiceId ASC (ties broken alphabetically).
// Run: node logic/tests.mjs
export function summarizeUsage(events) {
  // TODO
  return []
}
