// TASK: merge overlapping intervals.
// Input: array of [start, end] pairs (start <= end), not necessarily sorted.
// Output: array of merged, non-overlapping intervals, sorted by start.
// Touching intervals count as overlapping, e.g. [1,3] and [3,5] merge to [1,5].
export function mergeIntervals(intervals) {
  // TODO
  return []
}
