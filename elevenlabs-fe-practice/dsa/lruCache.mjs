// TASK: implement an LRU (Least Recently Used) cache with fixed capacity.
//   new LRUCache(capacity)
//   .get(key)        -> value, or -1 if not present. Counts as a "use" (refreshes recency).
//   .put(key, value)  -> insert/update. If over capacity, evict the LEAST recently used entry.
// Both operations should be O(1) average time. Hint: JS Map preserves insertion order,
// and re-inserting a key (delete + set) moves it to the "most recent" end.
export class LRUCache {
  constructor(capacity) {
    // TODO
  }
  get(key) {
    // TODO
    return -1
  }
  put(key, value) {
    // TODO
  }
}
