// YOUR WORK — implement solution(queries).
// Model an in-memory file system as a tree of nodes (dir or file), OOP-style.
// See ../README.md for the exact operation contract per level.
export function solution(queries) {
  const results = []
  // TODO: implement
  for (const q of queries) results.push("")
  return results
}
