# Client (browser) — safe to expose; RLS protects the data
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Server (session endpoint) — never exposed to the browser
ELEVENLABS_API_KEY=sk_...
PERCH_DEFAULT_AGENT_ID=agent_...
